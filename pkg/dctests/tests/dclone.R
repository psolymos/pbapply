## dclone documentation checks
## dontrun pieces indicated
library(dclone)
example(bugs.fit) # dontrun
example(bugs.parfit) # dontrun
example(clusterSeed)
example(clusterSize)
example(clusterSplitSB) # dontrun
example(coef.mcmc.list) # dontrun
example(dc.fit) # dontrun
example(dc.parfit) # dontrun
example(dclone)
example(dcoptions)
example(dctable) # dontrun
example(errlines)
example(jags.fit) # dontrun
example(jags.parfit) # dontrun
example(lambdamax.diag)
example(make.symmetric)
example(mcmcapply)
example(nclones)
example(ovenbird)
example(regmod)
example(snowWrapper) # dontrun
example(update.mcmc.list) # dontrun
example(write.jags.model)

example(bugs.fit) # dontrun
example(bugs.parfit) # dontrun
example(clusterSplitSB) # dontrun
example(coef.mcmc.list) # dontrun
example(dc.fit) # dontrun
example(dc.parfit) # dontrun
example(dctable) # dontrun
example(jags.fit) # dontrun
example(jags.parfit) # dontrun
example(snowWrapper) # dontrun
example(update.mcmc.list) # dontrun
